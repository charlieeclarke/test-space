query PostItem($slug: ID!) {
  PostItem(id: $slug) {
    content {
      _editable
      component
      content
    }
    slug
    name
    first_published_at
    full_slug
    uuid
    alternates {
      fullSlug
      id
      name
      published
      slug
    }
  }
}
